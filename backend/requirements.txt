# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Validation / settings ---
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Database (async) ---
sqlalchemy[asyncio]==2.0.36
aiomysql==0.2.0
cryptography==44.0.0  # MySQL 8.x caching_sha2_password için gerekli
alembic==1.14.0

# --- Cache / queue ---
redis==5.2.1
celery==5.4.0

# --- Auth / security ---
pyjwt==2.10.1
passlib==1.7.4
bcrypt==4.0.1  # passlib 1.7.4 ile uyumlu son sürüm; 4.1+ AttributeError verir
python-multipart==0.0.20

# --- Files / data ---
pandas==2.2.3
openpyxl==3.1.5
aiofiles==24.1.0
httpx==0.28.1
Pillow==11.0.0  # avatar upload (resize + format dönüşüm)

# --- PDF report rendering ---
weasyprint==63.1
jinja2==3.1.4

# --- Rate limiting ---
slowapi==0.1.9

# --- E-posta (Gmail SMTP / aiosmtplib) ---
aiosmtplib==3.0.2

# --- Logging ---
structlog==24.4.0

# --- Dev / test ---
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
ruff==0.8.4
