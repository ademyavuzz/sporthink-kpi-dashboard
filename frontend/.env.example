# -----------------------------------------------------------------------------
# Sporthink Frontend — Environment Variables
# -----------------------------------------------------------------------------
# Vite sadece VITE_ prefix'li değişkenleri client bundle'a expose eder.
# Hassas bilgi (API key, secret) bu dosyaya YAZILMAZ — bundle'a girer.
# -----------------------------------------------------------------------------

VITE_API_BASE_URL=http://localhost:8000/api/v1
